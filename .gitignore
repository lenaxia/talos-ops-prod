# Trash
.DS_Store
Thumbs.db
# k8s
kubeconfig
talosconfig
.decrypted~*.yaml
.config.env
*.agekey
*.pub
*.key
# Talos generated cluster config (contains unencrypted bootstrap tokens,
# etcd secretbox encryption secret, machine certs, and CA keys). NEVER commit.
kubernetes/bootstrap/talos/clusterconfig/
kubernetes/bootstrap/talos/clusterconfig-backup-*/
kubernetes/bootstrap/talos/talsecret.yaml
kubernetes/bootstrap/talos/temp.yaml
# Private
.private
.bin
# Ansible
.venv*
# Taskfile
.task
# Brew
Brewfile.lock.json
# intellij
.idea
# wiki
wiki
# Bootstrap
/config.yaml
.aider*
config.*.yaml
venv
.tmp
