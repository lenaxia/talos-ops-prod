---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: msedge-config
  namespace: home
  labels:
    snapshot.home.arpa/enabled: "true"
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi
