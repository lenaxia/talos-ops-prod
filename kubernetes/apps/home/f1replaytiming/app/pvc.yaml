---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: f1replaytiming-data
  namespace: home
  annotations:
    longhorn.io/numberOfReplicas: "1"
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 20Gi
