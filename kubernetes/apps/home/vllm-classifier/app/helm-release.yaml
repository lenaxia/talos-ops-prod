# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &appname vllm-classifier
  namespace: &namespace home
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 5.0.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  install:
    createNamespace: true
    timeout: 15m
    remediation:
      retries: 10
  upgrade:
    timeout: 15m
    remediation:
      retries: 10
  values:
    defaultPodOptions:
      nodeSelector:
        node-role.kubernetes.io/worker: 'true'
      tolerations:
        - key: "dedicated"
          operator: "Equal"
          value: "special"
          effect: "NoSchedule"
    controllers:
      app:
        type: deployment
        annotations:
          reloader.stakater.com/auto: 'true'
        containers:
          app:
            image:
              repository: vllm/vllm-openai
              tag: 
                v0.25.1@sha256:e4f88a835143cd22aee2397a26ec6bb80b3a4a6fe0c882bcbc63822904766089
            command: ["/bin/sh", "-c"]
            args:
              - |
                vllm serve \
                    cyankiwi/gemma-4-12B-it-AWQ-INT4 \
                    --served-model-name classifier \
                    --max-model-len 5000 \
                    --max-num-seqs 10 \
                    --gpu-memory-utilization 0.90 \
                    --tensor-parallel-size 1 \
                    --trust-remote-code \
                    --enable-auto-tool-choice \
                    --tool-call-parser gemma4 \
                    --host 0.0.0.0 \
                    --port 8000 \
                    --dtype auto
                # NOTE: --disable-log-requests was removed in vllm v0.20
                # (replaced by opt-in --enable-log-requests; default is no request logging).
            env:
              - name: TZ
                value: ${TIMEZONE}
              - name: NVIDIA_VISIBLE_DEVICES
                value: "GPU-bec81ab7-67a6-cfeb-98d0-dd3f8599e337" # 3090
              - name: CUDA_LAUNCH_BLOCKING
                value: 0
              - name: TORCH_USE_CUDA_DSA
                value: 1
              - name: VLLM_USE_MODELSCOPE
                value: "false"
              - name: VLLM_TORCH_COMPILE
                value: "true"
            envFrom:
              - secretRef:
                  name: *appname
            probes:
              liveness: &probe
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /health
                    port: 8000
                  initialDelaySeconds: 300
                  periodSeconds: 30
                  successThreshold: 1
                  failureThreshold: 5
                  timeoutSeconds: 10
              readiness: *probe
            resources:
              requests:
                nvidia.com/gpu: "1"
                memory: "16Gi"
              limits:
                nvidia.com/gpu: "1"
                memory: "22Gi"

    persistence:
      data:
        enabled: true
        type: persistentVolumeClaim
        storageClass: openebs-hostpath
        accessMode: ReadWriteOnce
        size: 20Gi
        globalMounts:
          - path: /root/.cache/huggingface
      shm:
        type: emptyDir
        medium: Memory
        sizeLimit: 8Gi
        globalMounts:
          - path: /dev/shm
    service:
      app:
        ports:
          http:
            port: 8000
        primary: true
        type: LoadBalancer
        loadBalancerIP: ${SVC_VLLM_CLASSIFIER_IP}
