# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/app-template-4.6.2/charts/other/app-template/values.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &appname robrowserlegacy
  namespace: ragnarok
spec:
  interval: 5m
  chart:
    spec:
      chart: app-template
      version: 4.3.0
      interval: 16m
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  values:
    defaultPodOptions:
      nodeSelector:
        node-role.kubernetes.io/worker: 'true'

    controllers:
      main:
        strategy: Recreate
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          main:
            image:
              repository: ghcr.io/lenaxia/robrowserlegacy
              tag: "1.1.5"
              pullPolicy: IfNotPresent
            env:
              TZ: ${TIMEZONE}
              # Must be the public URL so the embedded asset/WS server generates correct URLs
              CLIENT_PUBLIC_URL: "https://ro.${SECRET_DEV_DOMAIN}"
              # Allow the WS proxy to forward to the rAthena server (non-host-network deployment).
              # Uses MetalLB IP (not ClusterIP DNS) because rAthena runs with hostNetwork: true,
              # which makes ClusterIP routing non-functional.
              WS_ALLOWED_TARGETS: "192.168.5.145:6900,192.168.5.145:6121,192.168.5.145:5121"
            resources:
              requests:
                cpu: 50m
                memory: 256Mi
              limits:
                memory: 1Gi

    service:
      main:
        primary: true
        controller: main
        ports:
          http:
            port: 3338

    ingress:
      main:
        enabled: true
        className: traefik
        annotations:
          cert-manager.io/cluster-issuer: letsencrypt-production
          traefik.ingress.kubernetes.io/router.entrypoints: websecure
          traefik.ingress.kubernetes.io/router.middlewares: networking-chain-authelia@kubernetescrd
          hajimari.io/enable: "true"
          hajimari.io/icon: gamepad-variant
          hajimari.io/info: *appname
          hajimari.io/group: Ragnarok
        hosts:
          - host: &uri ro.${SECRET_DEV_DOMAIN}
            paths:
              - path: /
                pathType: Prefix
                service:
                  identifier: main
                  port: http
        tls:
          - hosts:
              - *uri
            secretName: *uri

    # Config.local.js is managed as a ConfigMap and mounted into the container.
    # Edit the configMaps.config.data section below to adjust server settings.
    configMaps:
      config:
        enabled: true
        data:
          Config.local.js: |
            /* roBrowserLegacy runtime configuration — loaded by the browser */
            window.ROConfigLocal = {
              servers: [{
                display:    'Kao RO Renewal',
                address:    '192.168.5.145',
                port:       6900,
                packetver:  20200401,
                renewal:    true,
                socketProxy: 'wss://ro.${SECRET_DEV_DOMAIN}/ws/',
              }],
              /* GRF asset server — served by this container */
              remoteClient:    'https://ro.${SECRET_DEV_DOMAIN}/',
              /* Critical for any non-localhost / NAT / containerised deployment */
              forceUseAddress: true,
              skipIntro:       false,
              skipServerList:  false,
            };

    persistence:
      config:
        type: configMap
        identifier: config
        advancedMounts:
          main:
            main:
              - path: /robrowser/Config.local.js
                subPath: Config.local.js
                readOnly: true
